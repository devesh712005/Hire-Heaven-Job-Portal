import { AccountProps } from "@/components/type";
import React from "react";

const info: React.FC<AccountProps> = ({ user, isYourAccount }) => {
  return <div className="max-w-5xl mx-auto px-4 py-8">info</div>;
};

export default info;
